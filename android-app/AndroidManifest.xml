<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.deepseek.harness"
    android:versionCode="37"
    android:versionName="1.13.10">

    <uses-sdk android:minSdkVersion="24" android:targetSdkVersion="28" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- 定时任务：精确闹钟（Android 12+ 需要；setAlarmClock 不受此权限限制，双保险） -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />

    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:hardwareAccelerated="true"
        android:requestLegacyExternalStorage="true">

        <meta-data
            android:name="moe.shizuku.client.V3_SUPPORT"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="fullUser"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/AppTheme">
            <!-- v1.13.7 问题⑥：原来写的是 unspecified —— 语义是「我不表态，系统看着办」，
                 真机上表现为**沿用当前屏幕旋转**：从横屏的游戏/视频切过来，App 就一直是横屏，
                 哪怕手机已经竖回来了（跟随的是「上一个应用」，不是系统设置）。
                 改成 fullUser：== 系统设置里「自动旋转」开着 → 跟随重力感应（含反向横竖屏）；
                 关着（锁定方向）→ 跟随用户锁定，不会被上一个 App 带跑。 -->
            <!-- v1.13.7 问题④：原主题 @android:style/Theme.Black.NoTitleBar.Fullscreen
                 会隐藏状态栏，ColorOS 上窗口还被下移 133px → 顶部一块黑条。
                 改为 res/values/styles.xml 里的 AppTheme（父主题 Theme.Black.NoTitleBar，
                 不隐藏状态栏，状态栏底色 = App 深色底）。 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 前台保活服务：引擎运行期间常驻通知栏，挂后台不被杀 -->
        <service
            android:name=".EngineService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- 定时任务闹钟接收器（⑥）：AI 设置定时后由系统闹钟唤醒，即使 App 被杀也能触发 -->
        <receiver
            android:name=".AlarmReceiver"
            android:exported="false" />

        <!-- 小鲸鱼悬浮窗（含引擎状态显示） -->
        <service
            android:name=".OverlayService"
            android:exported="false" />

        <!-- 虚拟屏接入桥：启动 Operit server + HTTP->binder 转发（插件 API） -->
        <service
            android:name=".VsreenBridgeService"
            android:exported="false" />

        <!-- v1.7 无障碍服务：AI 读屏 + 模拟操作 + 无障碍截图（用户需在系统设置开启） -->
        <service
            android:name=".AccessibilityService"
            android:exported="false"
            android:label="DeepSeek Harness 屏幕助手"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_config" />
        </service>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="com.deepseek.harness.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!-- v1.13：「分享日志」以**文件**形式发出（targetSdk 28 不允许直传 file:// URI，
             本项目无 androidx，故自实现最小只读 ContentProvider，只暴露 cache/share 下的文件） -->
        <provider
            android:name=".LogShareProvider"
            android:authorities="com.deepseek.harness.logshare"
            android:exported="false"
            android:grantUriPermissions="true" />
    </application>
</manifest>
